包序列与完整性：尽管 TCP 保证了数据包的有序传输，但在应用层仍然需要保证文件的完整性，可以在文件传输过程中增加校验和（例如 MD5、SHA）来验证文件的完整性
1.文件传输的开始与结束
    在传输文件时，必须定义好文件传输的开始和结束标识，特别是在应用层：
    传输开始标志：例如，可以传输一个文件头，其中包含文件的大小、文件名等信息，告诉接收方将要接收的文件数据大小。
    传输结束标志：发送完文件后，需要明确告知接收方传输结束，可以通过一个特殊的结束标识（如结束符）来指示文件传输的完成。